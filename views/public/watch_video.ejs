<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %> - Video Player
    </title>
    <link rel="stylesheet" href="/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Hubot+Sans:wght@400;600;700;800&display=swap');

        :root {
            --bg-dark: #0b0e14;
            --card-bg: #151921;
            --accent-red: #e11d48;
            --text-main: #f8fafc;
            --text-dim: #94a3b8;
            --border: rgba(255, 255, 255, 0.08);
        }

        body {
            background-color: var(--bg-dark);
            background-image:
                radial-gradient(circle at 50% 0%, rgba(30, 41, 59, 0.5) 0%, transparent 75%),
                linear-gradient(rgba(255, 255, 255, 0.02) 1px, transparent 1px),
                linear-gradient(90deg, rgba(255, 255, 255, 0.02) 1px, transparent 1px);
            background-size: 100% 100%, 40px 40px, 40px 40px;
            color: var(--text-main);
            margin: 0;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            font-family: 'Hubot Sans', sans-serif;
            -webkit-font-smoothing: antialiased;
        }

        .top-nav {
            padding: 2rem;
            max-width: 1200px;
            margin: 0 auto;
            width: 100%;
            box-sizing: border-box;
        }

        .back-btn {
            background: #272c36;
            color: var(--text-main);
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            font-weight: 600;
            font-size: 0.9rem;
            padding: 0.75rem 1.25rem;
            border-radius: 10px;
            border: 1px solid var(--border);
            transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .back-btn:hover {
            background: #323945;
            transform: translateY(-2px);
            border-color: rgba(255, 255, 255, 0.2);
            box-shadow: 0 10px 20px -10px rgba(0, 0, 0, 0.5);
        }

        .main-content {
            flex: 1;
            display: flex;
            align-items: flex-start;
            justify-content: center;
            padding: 0 2rem 4rem;
        }

        .video-card {
            width: 100%;
            max-width: 900px;
            background: var(--card-bg);
            border-radius: 20px;
            overflow: hidden;
            border: 1px solid var(--border);
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.7);
        }

        .video-player-container {
            width: 100%;
            aspect-ratio: 16/9;
            background: #000;
            position: relative;
        }

        .video-player-container iframe {
            width: 100%;
            height: 100%;
            border: none;
        }

        .video-info {
            padding: 2.5rem;
        }

        .video-badge {
            display: inline-flex;
            align-items: center;
            gap: 6px;
            background: var(--accent-red);
            color: white;
            font-size: 0.7rem;
            font-weight: 800;
            text-transform: uppercase;
            padding: 5px 10px;
            border-radius: 4px;
            margin-bottom: 1.25rem;
            letter-spacing: 0.05em;
        }

        .video-title {
            margin: 0 0 1rem;
            font-size: 2rem;
            font-weight: 800;
            line-height: 1.2;
            letter-spacing: -0.03em;
        }

        .video-desc {
            color: var(--text-dim);
            font-size: 1rem;
            line-height: 1.6;
            margin-bottom: 2rem;
            border-bottom: 1px solid var(--border);
            padding-bottom: 2rem;
        }

        .share-section {
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .share-label {
            font-size: 0.85rem;
            font-weight: 600;
            color: var(--text-dim);
        }

        .share-icons {
            display: flex;
            gap: 8px;
        }

        .share-btn {
            width: 36px;
            height: 36px;
            display: flex;
            align-items: center;
            justify-content: center;
            background: #272c36;
            border: 1px solid var(--border);
            border-radius: 8px;
            color: var(--text-main);
            font-size: 0.9rem;
            text-decoration: none;
            transition: all 0.2s;
        }

        .share-btn:hover {
            background: #323945;
            color: white;
            border-color: rgba(255, 255, 255, 0.2);
        }

        @media (max-width: 768px) {
            .top-nav {
                padding: 1.5rem;
            }

            .main-content {
                padding: 0 1rem 2rem;
            }

            .video-info {
                padding: 1.5rem;
            }

            .video-title {
                font-size: 1.5rem;
            }

            .video-desc {
                font-size: 0.9rem;
            }

            .back-btn span {
                display: none;
            }

            .back-btn {
                padding: 0.6rem;
                border-radius: 8px;
            }
        }
    </style>
</head>

<body>
    <nav class="top-nav">
        <a href="javascript:history.back()" class="back-btn">
            <i class="fa-solid fa-arrow-left"></i>
            <span>All Videos</span>
        </a>
    </nav>

    <main class="main-content">
        <div class="video-card">
            <div class="video-player-container">
                <iframe src="<%= videoUrl %>"
                    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
                    allowfullscreen></iframe>
            </div>

            <div class="video-info">
                <div class="video-badge">
                    <i class="fa-solid fa-bolt"></i>
                    Video Content
                </div>

                <h1 class="video-title">
                    <%= title %>
                </h1>

                <div class="video-desc">
                    Experience the full presentation and technical demonstration of <%= title %>. For more information
                        and additional resources, please contact our support team.
                </div>

                <div class="share-section">
                    <span class="share-label">Share:</span>
                    <div class="share-icons">
                        <a href="#" class="share-btn"><i class="fa-brands fa-x-twitter"></i></a>
                        <a href="#" class="share-btn"><i class="fa-brands fa-facebook-f"></i></a>
                        <a href="#" class="share-btn"><i class="fa-brands fa-linkedin-in"></i></a>
                        <a href="#" class="share-btn"><i class="fa-solid fa-link"></i></a>
                    </div>
                </div>
            </div>
        </div>
    </main>
</body>

</html>